<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- VideoRecorder 直接用 Camera2 API 在应用内录制视频，需要相机权限；上报数据经 CXR-S sendMessage 走网络。 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.EchoGlasses">

        <!-- LAUNCHER 使 Echo 出现在眼镜应用列表；会话由手机端 CXR-L appStart 主导拉起。 -->
        <!-- 眼镜内置屏物理方向为竖屏 480x640，直接按原生竖屏渲染：人眼与录屏均为正向，无需再手动旋转。 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
